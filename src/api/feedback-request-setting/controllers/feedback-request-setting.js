'use strict';

/**
 * feedback-request-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::feedback-request-setting.feedback-request-setting');
