'use strict';

/**
 * feedback-request-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::feedback-request-setting.feedback-request-setting');
